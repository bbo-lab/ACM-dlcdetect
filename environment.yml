name: bbo_acm_dlcdetect
channels:
  - defaults
dependencies:
  - numpy
  - python=3.7
  - matplotlib
  - pip
  - ffmpeg
  - tensorflow<2.0
  - pip:
    - argparse
    - deeplabcut==2.1.10.4 #DLC used originally: 2.1.6.4, but I hope minor version is sufficient
    - scikit-image
